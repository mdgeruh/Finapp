{
  "name": "finapp",
  "version": "1.1.25",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "node -e 'process.exit(0)'",
    "lint": "node -e 'require(\"child_process\").execSync(\"for f in *.js; do node -c $f; done\", {stdio: \"inherit\"})'"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
